# Styling & conventions

> Template. Naming, formatting and commit conventions `create-pr` and `tdd` should follow.

## Code
- Formatter / linter: `...` (the tool, not hand-rules tooling already enforces).
- Naming: files, classes, use cases, tests.

## Commits
- Format: e.g. `type(scope): imperative summary`.
- One behavior per commit where possible.

## Branches
- Naming convention: e.g. `type/short-slug`. (`develop` phase D creates the branch by hand
  following this.)

## Pull requests
- Required reviewers / labels / CI checks, if any. (`create-pr` reads this; it hardcodes none.)
